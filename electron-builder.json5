// @see - https://www.electron.build/configuration/configuration
{
  $schema: 'https://raw.githubusercontent.com/electron-userland/electron-builder/master/packages/app-builder-lib/scheme.json',
  // FIX: Updated App ID
  appId: 'com.ledgerly.app',
  // FIX: Updated Product Name (this is what shows in Start Menu/Add Remove Programs)
  productName: 'Legerly',
  directories: {
    output: 'release/${version}',
  },
  // Speed up packaging by skipping rebuild here; native deps are handled by postinstall/ensure scripts.
  npmRebuild: false,
  files: ['dist/**/*', 'dist-electron/**/*'],
  win: {
    target: [
      {
        target: 'nsis',
        arch: ['x64'],
      },
    ],
    icon: 'public/icon.ico',
    signAndEditExecutable: false,
  },
  fileAssociations: [
    {
      ext: 'biz',
      name: 'Ledgerly Business Document',
      description: 'Ledgerly SQLite business data file',
      role: 'Editor',
      icon: 'public/icon.ico',
    },
  ],
  nsis: {
    oneClick: false,
    allowToChangeInstallationDirectory: true,
    createDesktopShortcut: true,
    createStartMenuShortcut: true,
    // FIX: Added shortcut name
    shortcutName: 'Legerly',
  },
  extraResources: [
    {
      from: 'node_modules/better-sqlite3',
      to: 'better-sqlite3',
      filter: ['**/*.node'],
    },
    {
      from: 'node_modules/keytar',
      to: 'keytar',
      filter: ['**/*.node'],
    },
  ],
}
